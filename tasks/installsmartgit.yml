   - name: Install latest git
     action: apt name=git state=latest update_cache=no default_release=wheezy-backports
   - name: Install smartgit
     action: apt name={{item}} state=present update_cache=no
     with_items:
       - openjdk-8-jdk
       - openjdk-8-jre
       - smartgit
